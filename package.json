{
  "name": "portfolio-api-test-pack",
  "private": true,
  "version": "0.1.0",
  "description": "Allure 3 companion config for the portfolio API pytest suite",
  "devDependencies": {
    "allure": "^3.0.0-beta.18"
  },
  "scripts": {
    "allure:run": "allure run -- python -m pytest",
    "allure:generate": "allure generate allure-results",
    "allure:open": "allure open allure-report"
  }
}
